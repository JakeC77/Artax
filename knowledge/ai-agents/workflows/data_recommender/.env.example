# Data Scope Recommendation Workflow - Environment Variables

# Required for live workflow testing
WORKSPACE_ID=123e4567-e89b-12d3-a456-426614174000
TENANT_ID=your-tenant-id

# Optional - GraphQL endpoint (defaults to Config.GRAPHQL_ENDPOINT if not set)
# GRAPHQL_ENDPOINT=https://your-api.com/graphql

# Optional - Authentication (usually set globally in app/.env)
# GRAPHQL_AUTH_ENABLED=true
# GRAPHQL_AUTH_SCOPE=api://your-app-id/.default

# Optional - Model selection for testing
# MODEL=gpt-4o
# MODEL=gpt-4o-mini

# Azure OpenAI Configuration (if using Azure instead of OpenAI)
# AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com
# AZURE_OPENAI_DEPLOYMENT_NAME=gpt-4o
# AZURE_OPENAI_API_VERSION=2024-06-01
# AZURE_OPENAI_API_KEY=your-api-key-here
